package com.openbox.realcomm3.controls;

import android.content.Context;
import android.util.AttributeSet;
import android.widget.TabWidget;

public class ScheduleTabWidget extends TabWidget
{
	public ScheduleTabWidget(Context context)
	{
		super(context);
	}

	public ScheduleTabWidget(Context context, AttributeSet attrs)
	{
		super(context, attrs);
	}

	public ScheduleTabWidget(Context context, AttributeSet attrs, int defStyle)
	{
		super(context, attrs, defStyle);
	}

	// TODO see if i can use a custom layout here, or just leave it as is for now, and fiddle when theres time?
}
